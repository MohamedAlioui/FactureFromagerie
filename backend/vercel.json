{
  "installCommand": "npm install --legacy-peer-deps",
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    },
    {
      "dest": "/api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "/" }]
}
